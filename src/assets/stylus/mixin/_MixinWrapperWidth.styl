MixinWrapperWidth(contentwidth, globalwidth= MixinBreakPoint(MAXPC), center= ture)
  if globalwidth == full
    width percentage(contentwidth / MixinBreakPoint(MAXPC))
    max-width 100%
  else
    width percentage(contentwidth / globalwidth)
    max-width contentwidth
  if center == ture 
    margin-left auto
    margin-right auto
  +sp-only()
    if globalwidth == MixinBreakPoint(MAXPC) || globalwidth == full
      width calc(100% - 40px)
    else
      width 100%